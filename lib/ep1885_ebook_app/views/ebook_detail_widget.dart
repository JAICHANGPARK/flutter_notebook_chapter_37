import 'package:flutter/material.dart';

class EbookDetailWidget extends StatelessWidget {
  const EbookDetailWidget({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
